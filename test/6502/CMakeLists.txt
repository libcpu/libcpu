PROJECT(test_6502)

SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${LIBCPU_RUNTIME_OUTPUT_DIRECTORY})
IF(WIN32)
	SET(WIN32_SRCS readdir_win32.c)
ELSE()
	SET(WIN32_SRCS)
ENDIF()
ADD_EXECUTABLE(test_6502 main.cpp cbmbasic_lib.cpp ${WIN32_SRCS})
TARGET_LINK_LIBRARIES(test_6502 cpu)
